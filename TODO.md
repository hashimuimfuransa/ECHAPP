# TODO

- [ ] Add auth-based route guard so reopening can’t land on `/auth-selection` when session exists.
- [ ] Adjust `AuthSelectionScreen` navigation timing condition to avoid race on session restore.
- [ ] Run Flutter analyze / tests (at least `flutter test` if feasible).

