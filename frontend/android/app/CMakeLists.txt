# Dummy CMakeLists.txt to prevent automatic CMake configuration
cmake_minimum_required(VERSION 3.4.1)

# This is a minimal CMake file to satisfy the build system
# without triggering complex native compilation